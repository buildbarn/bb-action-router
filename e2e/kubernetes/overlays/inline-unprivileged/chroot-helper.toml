# The worker's build directory is /runner/build (see base/config/worker.jsonnet),
# so /runner has to stay visible: it holds the action's working directory and, in
# inline mode, the merged image root itself.
keep-dirs = ["runner"]

[build-user]
uid = 0
gid = 0
